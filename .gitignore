# binaries
# 必须带前导 /:不带斜杠的 pocketdrive 会匹配任意层级的同名目录,
# 把 cmd/pocketdrive/ 整个主包也一并忽略掉。
/pocketdrive.exe
/pocketdrive
/out/

# runtime data
/data/
*.db
*.db-*

# frontend
web/node_modules/
web/dist/
web/shots/

# misc
.DS_Store
server.log
cookies.txt

# local
.claude/
# 同理带前导 /,否则会连 internal/thumbs/ 源码包一起忽略
/thumbs/
ck.txt
